package com.example.mapper.helper;

/**
 * This is an interface with two methods that must be override
 * @author Rhane Mercado
 */
public interface RequestListener {
    void success();
    void failed();
}
